# Every kind of line a procedure can have.
#
# Blank lines and comments are not counted against the line limit. There are
# eight lines: two guards, three commands, a wait, a condition and the command
# it guards.

on-error continue
noop
info

# Not a command. With on-error continue the line fails and the run goes on.
nonsense_command

on-error stop
wait 1

# Source 9, id 9 was never recorded, so the next line is skipped.
if-event 9 9 skip
noop
