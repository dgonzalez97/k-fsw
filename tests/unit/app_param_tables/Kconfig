# The application thread deadline, which the system table validates against.
config KFSW_APP_HEALTH_DEADLINE_MS
	int "How long the application thread may go without reporting"
	default 4000
	range 200 60000

source "Kconfig.zephyr"
