CONFIG_ZTEST=y

CONFIG_KFSW_RESMON=y
CONFIG_KFSW_RESMON_ALERT_PERCENT=90

# Longer than the suite, so the periodic sweep cannot race a test that is
# sweeping on purpose.
CONFIG_KFSW_RESMON_PERIOD_MS=3600000
