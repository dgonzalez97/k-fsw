# Wall clock that can be set over the link. libcsp already has the exchange,
# so this only adds the RTC.
CONFIG_COUNTER=y
CONFIG_RTC=y
CONFIG_POSIX_API=y
CONFIG_POSIX_TIMERS=y
CONFIG_POSIX_CLOCK_SELECTION=y
