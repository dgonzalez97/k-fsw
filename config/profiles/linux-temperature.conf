# The temperature example without a sensor, which is all native_sim can offer:
# table 51 registers and reports the reserved value, so the ground path can be
# exercised end to end without pretending a reading exists.
CONFIG_KFSW_TEMPERATURE_SENSOR_EXAMPLE=y
CONFIG_KFSW_TEMP_EXAMPLE_SHELL=y
